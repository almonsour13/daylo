import { Activity } from "@/shared/types/activity";
import { View } from "react-native";

export default function ActivityCard({ activity }: { activity: Activity }) {
    return <View className="p-4"></View>;
}
